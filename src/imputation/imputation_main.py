"""The main file for the Imputation module."""
import logging
import pandas as pd


ImputationMainLogger = logging.getLogger(__name__)


def run_imputation(
    df: pd.DataFrame,
) -> pd.DataFrame:

    return df
